function unitTest() {
	log('------------- unit test begins -------------')
	
	/*
		remove from array method
	*/
	// var array = [2, 3, 4, 8, 19, 31];

	// log(array)
	// var array2 = removeFromArray(array, 3);

	// log(array)

	/*
		load json files
	*/
	// loadJSON('res/tpd.json', function(response){
	// 	var tpd = JSON.parse(response);
	// 	log(tpd)
	// })

	log('------------- unit test ends -------------')
}